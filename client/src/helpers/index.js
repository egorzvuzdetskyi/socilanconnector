import { setAuthToken } from "./setAuthToken";
import { getLocalStorageName } from "./localStorageName.map";
import { isEmpty } from "./is-empty";
import { isTokenExpired } from "./isTokenExpired";

export {
    setAuthToken,
    getLocalStorageName,
    isEmpty,
    isTokenExpired
}